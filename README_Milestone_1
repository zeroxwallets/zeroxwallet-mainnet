# ZeroxWallet – Milestone 1 Submission

Objective
The goal of Milestone 1 was to successfully deploy and demonstrate a functional non-custodial wallet on the Hedera Testnet, showcasing core features such as account integration, transactions, and smart contract deployment.

Achievements

✅ Live Wallet Deployment on Hedera Testnet
ZeroxWallet is now operational on the Hedera Testnet with a working user interface.

✅ Core Wallet Functions

Real-time HBAR balance check using Hedera Mirror Node APIs.

Send & Receive HBAR securely through Hedera Account IDs.

Transaction History integrated within the wallet UI.

QR Code generation for seamless fund receiving.


✅ Authentication & Security
Wallet creation and storage are handled via Firebase authentication, ensuring secure storage of account credentials during testing.

✅ Smart Contract Deployment
A sample smart contract was deployed and verified on the Hedera EVM using Remix, proving integration with Hedera’s contract ecosystem.

✅ Frontend + Backend Integration

Frontend built with Next.js (UI for wallet actions).

Backend APIs integrated with Firebase Admin SDK for account handling.



## ✅ Deliverables Completed (Milestone 1)

### 1. Wallet DApp (Frontend)
- Implemented using Next.js + Vercel (Testnet).
- Core features:
  - View wallet balance (HBAR).
  - Send HBAR to another account ID.
  - Receive HBAR (QR code + Account ID).
  - View transaction history.


### 2. Backend & APIs
- Integrated with Firebase Firestore for account management.
- APIs implemented:
  - **/api/balance** → Fetch user balance using Hedera SDK + Mirror Node.
  - **/api/send** → Send HBAR transaction (with private key signing).
  - **/api/history** → Load recent transactions from Mirror Node.


### 3. Smart Contract DeliverablHelloHederaelloHedera** Solidity contract deployed on Hedera JSON-RPC relay.
- Contract functionality:
  - Stores a message ("Hello, Hedera!" by default).
  - Supports updating the message via setMessage().
- Verified inteRemix IDE + MetaMask+ MetaMask**:
  - Deployed successfully on Hedera Testnet.
  - Updated contract state (message = "zeroxwallet test").
  - Transaction proof attached in screenshots.

### 4. EvidencWallet UI screenshotscreenshots** showing balance, send/receive, andAPI curl test logs test logs** verifying balance fetch and HBAR tSmart contract deployment screenshotscreenshots** vFirestore DB screenshotscreenshot** showing accountId + privateKey storage.


## 🛠️ TFrontend:*Frontend:** Next.js, TailBackend:**Backend:** Firebase Admin SDK, Firestore, Next.js APIBlockchain:lockchain:** Hedera Hashgraph (Testnet), Mirror Node, HedSmart Contract: Contract:** Solidity ^0.8.x, Remix IDE, MetaMask (Hedera JSON-RPC relay)


## 🚀 How to Test Locally
1. Clone the repository:
   `bash
   git clone <repo-url>
   cd zeroxwallet-testnet
